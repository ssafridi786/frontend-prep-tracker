specVersion: "v1"
serviceId: "frontend-prep"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "frontend-prep-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
